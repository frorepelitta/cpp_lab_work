Выполнить все задания в одном проекте в виде статических методов одного класса. \
В задании 1 и 2 бинарные файлы, содержат числовые данные, исходный файл необходимо заполнить \
случайными данными, заполнение организовать отдельным методом. \
В задании 3 бинарные файлы содержат величины типа struct, заполнение исходного файла \
необходимо организовать отдельным методом. \
В задании 4 в текстовом файле хранятся целые числа по одному в строке, исходный файл \
необходимо заполнить случайными данными, заполнение организовать отдельным методом. \
В задании 5 в текстовом файле хранятся целые числа по несколько в строке, исходный файл \
необходимо заполнить случайными данными, заполнение организовать отдельным методом. \
В задании 6 в текстовом файле хранится текст. \
Необходимо решить по 1 задаче из каждого задания согласно вашему варианту. Задания 1 и 4 \
оцениваются по 1 баллу, задания 2, 3, 5, 6 по 1,5 балла. Максимально за лабораторную работу можно \
получить 10 баллов (8 баллов за решение задач + 2 балла за оформление отчета).\
Вычислить произведение нечетных отрицательных компонент файла. \
Скопировать элементы заданного файла в квадратную матрицу размером n×n (если элементов \
файла недостает, заполнить оставшиеся элементы матрицы нулями). Поменять местами \
в каждом столбце минимальный и максимальный элементы. \
В файле найти сумму квадратов элементов. \
Вычислить произведение элементов. \
Переписать в другой файл строки, имеющие заданную длину m. 


Вся лабораторная работа сводится к работе с файлами - бинарными и текстовыми. \
Перед выполнением задания я реализовал общие методы - заполнение файла случайными числами и чтение файла.Случайные числа будут браться из модуля rand(). Для чтения файла и вывода его на экран просто будем при помощи цикла считывать файл. \
Для выполнения первого задания мы просто вызываем в некоторой последовательности статические методы класса лабораторной работы. \
Заполнение файла(случайными числами) -> вывод содержимого(в одном цикле) -> вызов метода, который принимает на вход название файла и выводит произведение отрицательных чисел файла, этот метод заходит в цикл по файлу и при помощи оператора ветвления находи подходящие под условия строки. \
Для случайной генерации - -12 -29 30 -24 37 -48 -30 -19 23 17 0 -31 39 5 25  Вывод это - -17081 , что является корректным выводом. \
Для случайной генерации - 29 17 43 1 -8 31 -18 -42 -47 -28 3 19 -41  Вывод это - 1927, что верно! \
Далее идет работа с матрицой -  идет ввод пользователем числа n, я создаю матрицу n×n из файла, которые по стандарту заполнены нулями. В отдельном методе делаю проход  в цикле по столбцам и нахожу min max в каждом столбце далее делаю их std::swap. \
При запуске ввожу n = 2 , создается матрица 2х2 из случайных элементов, сначала выводится иначальная матрица, а далее выводится матрица где элменты поменяны местами. \
<img width="310" height="198" alt="image" src="https://github.com/user-attachments/assets/8b786f30-e454-482c-8f3a-cd20056d119f" /> \
<img width="310" height="198" alt="image" src="https://github.com/user-attachments/assets/d0d55cbe-45de-44de-8476-db91d0ed0860" />

Потом текстовые файлы. Читаю из файла построчно числа и считаю сумму квадратов элементов. \
Для случайной генерации- 4, -4, 10, 8, 6, -10 получается вывод - 317, все верно \
Потом идет вычисление произведения элементов файла. \
Для случайной генерации- -2, 10, 2, -3, -5 имеем вывод - -600, что соответствует правде. \
Далее надо переписать строки длины m из одного файла в другой. \
Тут мы просто открываем два файла, один на чтение другой на запись и при помощи .length() считаем длину строки, если совпадает с m - записываем.
