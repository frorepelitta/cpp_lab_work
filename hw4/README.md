
Money \
Добавление произвольного количества \
копеек (unsigned int) к объекту типа Money.\
Результат должен быть типа Money.\
Сделаем класс Money с двумя полями для копеек и рублей. \
У класса сделаем три конструктора, с двумя параметрами, одни, и без параметров. \
Далее создадим функцию для добавления к экземпляру класса Money копеек, функция правилно обрабатывает количество добавленных копеек, если копеек больше 100 то лишние копейки буду добавлены в рубли. \
Далее переопределим префиксный и постфиксный инкримен и декремент,сначала переопределим префиксный вариант и через него будем переопределять постфиксный.\
Далее переопределим приведение к целому числовому и дробному типу данных для экземпляров класса Money, целый числовой тип будет выводить рубли, дробный тип будет выводить копейки в формате рублей. \
Далее переопределим бинарные операции сложения и вычитания, добавить нечего.\
Еще переопредлим поток вывода так, чтобы при выводе эксземпляра класса на вывод поступали переменные рублей и копеек. \

После того как мы выполнили задание мы сделаем ТЕСТЫ: \
1. Мы сделаем экземпляр класса в конструкторах: Money also_dengi(10) и Money dengi(10, 0), оба экземпляра класса создались без ошибок. \
2. Далее мы проверим перегрузку оператора вывода: мы введем в main std::cout << "money - " << dengi << std::endl; и получим вывод money - 10 0, отлично!\
3. Теперь мы протестируем функцию для создания экземпляров класса с клавиатуры create(): \
   Введем неправильное количество рублей:\
   Введите количество рублей - \
   -20\
   Ошибка ввода \
   Как видим программа отработала ошибку ввода корректно! \
   Теперь попробуем ввести правильное количество рублей и неправильное количество копеек: \
   Введите количество рублей - \
   10 \
   Введите количество копеек - \
   -10\
   Ошибка ввода \
   И тут тоже корректная работа! \
5. Далее протестируем постфиксный и префиксный инкримент, декремент тестировать не будем, т.к они реализуются , концептуально, одинаково. \
   Ввод: \
   std::cout << "++money - " << ++dengi << std::endl;\
   std::cout << "money++ - " << also_dengi++ << std::endl;\
   Вывод:\
   ++money - 10 1\
   money++ - 10 0\
6. Теперь протестируем явное приведение типа int и неявное приведение типа double: \
   Код (unsigned int)dengi Отработает корректно и вернет 10 \
   Код double double_new_money = new_money отработает корректно и поместит в переменную double_new_money количество копеек new_money  в формате рублей\
7. Протестируем оператор + для экземпляра класса Money:\
   Код new_money = new_money + 10 действительно прибавит к полю класса Money 10 копеек\
